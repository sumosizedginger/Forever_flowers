Forever Flowers: a moonlit flower field that grows on a phone screen, made as a gift.
Edit `src`, run `node build.mjs` to produce the single self contained `site/index.html`, and `node verify.mjs` to run the Playwright harness.
